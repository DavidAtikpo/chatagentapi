# Blueprint Render — repo API seul (racine = api/)
services:
  - type: web
    name: chatagent-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
